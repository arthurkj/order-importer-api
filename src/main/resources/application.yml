spring:
  application:
    name: order-importer-api
  datasource:
    url: jdbc:postgresql://0.0.0.0:5432/order-db
    username: user
    password: password
  jpa:
    show-sql: true

logging:
  level:
    br.com.akj.order: DEBUG

### LIQUIBASE ###
liquibase:
  enabled: true
  liquibase-schema: order-db
